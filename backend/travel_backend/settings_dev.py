# from .settings import *

# # Remove daphne if it's in INSTALLED_APPS
# if 'daphne' in INSTALLED_APPS:
#     INSTALLED_APPS.remove('daphne')

# # Ensure WSGI is used
# WSGI_APPLICATION = 'travel_backend.wsgi.application'

# # Remove ASGI_APPLICATION if it exists
# if hasattr(globals(), 'ASGI_APPLICATION'):
#     delattr(globals(), 'ASGI_APPLICATION')